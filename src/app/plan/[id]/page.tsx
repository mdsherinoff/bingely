export default function PlanPage() {
  return (
    <main className="bg-espresso min-h-screen px-6 py-12">
      {/* Selected media summary — poster + title + runtime */}
      <section className="mx-auto mb-10 max-w-4xl" />

      {/* Availability form */}
      <section className="mx-auto grid max-w-4xl gap-10 md:grid-cols-2">
        {/* Left — day/time selector */}
        <div />
        {/* Right — pace selector + generate button */}
        <div />
      </section>
    </main>
  )
}
